# GoReleaser configuration for promptarena-deploy-vertex
# See: https://goreleaser.com

version: 2

builds:
  # Only the adapter is released here. cmd/vertex-runtime ships as a container
  # image instead, published by .github/workflows/publish-runtime.yml when the
  # release is published — a loose binary would not help anyone, since Agent
  # Runtime pulls an image.
  - id: promptarena-deploy-vertex
    binary: promptarena-deploy-vertex
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/AltairaLabs/promptarena-deploy-vertex/internal/vertex.Version={{.Version}}
    mod_timestamp: '{{ .CommitTimestamp }}'

archives:
  # Bare binary (no tar.gz) named to match exactly what the promptarena
  # installer downloads: promptarena-deploy-vertex_<goos>_<goarch>.
  # {{ .Os }}/{{ .Arch }} are the raw GOOS/GOARCH the installer computes via
  # runtime.GOOS/runtime.GOARCH — no x86_64/title-case translation.
  - id: default
    formats: [binary]
    name_template: "promptarena-deploy-vertex_{{ .Os }}_{{ .Arch }}"

# An SBOM per released binary, attached to the GitHub release.
#
# A risk analyst asking a third-party questionnaire wants a document they can
# read, not an attestation they have to pull off an OCI manifest with buildx.
# The image carries its own SBOM as an OCI referrer; this is the same evidence
# in the form the question is actually asked in.
#
# SPDX JSON because it is what most questionnaires name. Generated from the
# artifacts GoReleaser just built, so the SBOM and the binary cannot drift.
# Named from .ArtifactName, which already carries GOOS/GOARCH.
#
# The obvious "${artifact}.spdx.sbom.json" does not work here: every platform
# builds a binary of the same name, so all four SBOMs collide on one filename
# and GoReleaser keeps one of them — describing a platform nobody can
# identify. It warns rather than fails, so this ships quietly broken.
sboms:
  - id: binaries
    artifacts: binary
    documents:
      - "{{ .ArtifactName }}.spdx.sbom.json"

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^ci:'

release:
  github:
    owner: AltairaLabs
    name: promptarena-deploy-vertex
  # Must stay false: `promptarena deploy adapter install vertex` fetches
  # releases/download/<tag>/<asset> anonymously, and draft releases are only
  # visible to users with push access — a draft publishes assets that 404 for
  # everyone the installer is meant to serve.
  draft: false
  name_template: "promptarena-deploy-vertex {{.Version}}"
