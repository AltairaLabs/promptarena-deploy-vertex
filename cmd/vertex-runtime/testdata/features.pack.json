{
  "$schema": "https://promptpack.org/schema/latest/promptpack.schema.json",
  "id": "vertex-features",
  "name": "Vertex Feature Pack",
  "version": "1.0.0",
  "description": "Exercises tools, validators and variables through the Agent Runtime contract",
  "template_engine": {
    "version": "v1",
    "syntax": "{{variable}}"
  },
  "prompts": {
    "support": {
      "id": "support",
      "name": "Support Agent",
      "version": "1.0.0",
      "system_template": "You are a support agent for {{company}}. Be concise.",
      "tools": ["lookup_order"],
      "validators": [
        {
          "type": "length",
          "enabled": true,
          "params": { "max_characters": 4000 }
        }
      ]
    }
  },
  "tools": {
    "lookup_order": {
      "name": "lookup_order",
      "description": "Look up an order by its id",
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {
            "type": "string",
            "description": "The order id to look up"
          }
        },
        "required": ["order_id"]
      }
    }
  }
}
