---
title: Reference
description: Field-by-field reference for the Agent Runtime adapter
---

Look-up material.

- [Configuration](./configuration/) — every deploy config field, its type,
  default and validation rules.
- [Resource types](./resource-types/) — what the adapter creates, and which
  lifecycle operations each type supports.
- [Environment variables](./environment-variables/) — every variable injected
  into the runtime container, and where its value comes from.
- [Runtime protocols](./runtime-protocols/) — the HTTP contract the container
  serves, including the streaming format.
